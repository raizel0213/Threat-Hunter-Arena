# Copy to .env.local for local dev overrides

# Backend API base URL — leave empty to default to http://127.0.0.1:8000
# When running via docker-compose this is baked in at build time as /api
# VITE_API_BASE_URL=http://127.0.0.1:8000
